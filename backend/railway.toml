[build]
builder = "NIXPACKS"

[deploy]
startCommand = "gunicorn gagcoffee.wsgi:application --bind 0.0.0.0:$PORT"
healthcheckPath = "/api/menu/"
healthcheckTimeout = 30
restartPolicyType = "ON_FAILURE"
